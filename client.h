
#include <string>

using namespace std;

int start(); //start the command line client.

int add(string name, string type, string file_path);

int get(string name, string type=0);

int search(string name, string type);



